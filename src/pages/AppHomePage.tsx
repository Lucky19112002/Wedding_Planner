import { EmptyState } from '@/components/ui/EmptyState';

export function AppHomePage() {
  return (
    <EmptyState
      title="App foundation"
      description="Protected routing and session persistence are ready for Phase 5 modules."
    />
  );
}
